---
# WARNING this file is managed by terraform and cannot be edited manually, see
# tf/github/geoadmin/modules/github-workflows-milestone/main.tf
name: on-pr

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited
      - labeled
      - unlabeled

jobs:
  pr-edit:
    uses: geoadmin/.github/.github/workflows/pr-auto-milestone.yml@master
