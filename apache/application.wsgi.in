#!/usr/bin/env python


from pyramid.paster import get_app, setup_logging

ini_path = "${INSTALLDIR}/${MODWSGI_CONFIG}" #use a full path cause it will be runned from apache directory
setup_logging(ini_path)
application = get_app(ini_path, 'main')
