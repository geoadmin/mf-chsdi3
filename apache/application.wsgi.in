#!/usr/bin/env python

from pyramid.paster import get_app


ini_path = "${INSTALL_DIR}/${MODWSGI_CONFIG}" #use a full path cause it will be runned from apache directory
application = get_app(ini_path, 'main')
